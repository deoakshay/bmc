apiVersion: v1
kind: Service
metadata:
  name: bmc
spec:
  selector:
    app: bmc-csv
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  selector:
    app: bmc-sqlite
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080    
